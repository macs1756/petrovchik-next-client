import { Metadata } from "next"


export const metadata: Metadata = {
  title: 'Work',
  description: 'Work',
}

export default function WorkCatalog() {
    return(
      <div>
        Work catalog
      </div>
    )
}